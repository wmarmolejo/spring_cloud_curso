spring.application.name=servicio-productos
server.port=${PORT:0}

eureka.instance.instance-id=${spring.application.name}:${spring.application.instance_id:${random.value}}

eureka.client.service-url.defaultZone=http://localhost:8761/eureka

spring.sleuth.sampler.probability=1.0


!--spring.datasource.url=jdbc:mysql//localhost:3306/db_springboot_cloud?serverTimezone=America/Bogota&allowPublicKeyRetrieval=true&useSSL=false
!--spring.datasource.username=root
!--spring.datasource.password=sasa
!--spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
!-- dialecto
!--spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect 
!--spring.jpa.hibernate.ddl-auto=create 
!--create-crea las tablas por el entity
!--create-drop -crea la tabla y al final destruye todo
!--update- crea la tabla si no hay nada, si hay modifica si encientra cambios de estructra en la bs

!--logging.level.org.hibernate.SQL=debug